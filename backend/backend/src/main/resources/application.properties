spring.application.name=backend

# Configuración del servidor
server.port=8080
server.address=0.0.0.0

# Configuración de la Base de Datos
spring.datasource.url=jdbc:mysql://localhost:3306/foodies_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de JPA/Hibernate (para que mapee las tablas automáticamente)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT
app.jwt.secret=cambia-esta-clave-super-secreta-para-savorsync-256bits-minimo-aaaaaaaa
app.jwt.expiration-ms=86400000

# Configuración de Gmail SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=rojasbotero@gmail.com
spring.mail.password=dwlm kvhw gceq mguy
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

